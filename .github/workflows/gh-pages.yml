name: Github User Pages Build


on:
  pull_request:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1

    - name: hugo
      uses: klakegg/actions-hugo@1.0.0

      # - name: Build
      #   run: hugo --minify

    - name: Deploy
      uses: klakegg/actions-hugo@1.0.0
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./public

      # - name: Setup Hugo
      #   uses: peaceiris/actions-hugo@v2.4.13
      #   with:
      #     hugo-version: 'latest'